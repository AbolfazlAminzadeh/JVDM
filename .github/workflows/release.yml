name: Release JVDM-UI Native

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: x86_64
            runner: ubuntu-24.04
            artifact: JVDM-linux-x86_64

          - os: macos
            arch: aarch64
            runner: macos-latest
            artifact: JVDM-macos-arm64

          - os: windows
            arch: x86_64
            runner: windows-2022
            artifact: JVDM-windows-x86_64.exe
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup GraalVM JDK 21 (with JavaFX support where possible)
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '25'                    # JavaFX 21 works best on 21
          distribution: 'graalvm'               # Oracle GraalVM
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        if: matrix.os != 'windows'

      - name: Build Shadow JAR
        run: ./gradlew :UI:shadowJar --no-daemon

      - name: Build Native Image
        run: ./gradlew :UI:nativeCompile --no-daemon
        env:
          GRADLE_OPTS: "-Xmx8g"

      - name: Prepare Artifact
        run: |
          mkdir -p artifacts
          if [[ "${{ matrix.os }}" == "windows" ]]; then
            cp build/native/nativeCompile/JVDM.exe "artifacts/${{ matrix.artifact }}"
          else
            cp build/native/nativeCompile/JVDM "artifacts/${{ matrix.artifact }}"
          fi
        shell: bash
        working-directory: UI

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: artifacts/${{ matrix.artifact }}
          retention-days: 30

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: artifacts/${{ matrix.artifact }}
          generate_release_notes: true
package org.kroj.Core.Tools.CompletableCombiner;

public class Combiner {
/*
    public static <A, B, C, R> CompletableFuture<R> combine3Future(
            CompletableFuture<A> f1,
            CompletableFuture<B> f2,
            CompletableFuture<C> f3,
            ThreeFuture<A, B, C, R> fn) {

        return f1.thenCombine(f2, (a, b) -> new Object[]{a, b})
                .thenCombine(f3, (ab, c) -> {
                    A a = (A) ab[0];
                    B b = (B) ab[1];
                    return fn.apply(a, b, c);
                });
    }
 */
}
